let count = 0;
let num = parseInt(prompt("Digite um número"));
let op = prompt("Digite a operação");
let result

if(op == "*"){
 while(count<=10){
      op =

    console.log(val)
    console.log(num)
    console.log(`${num} x ${val} = ${val * num}`)
    count++
}}else if(op == "+"){
    while(count<=10){
        console.log(`${num} + ${val} = ${val + num}`)
    count++
}}else if(op == "-"){
    while(count<=10){
        console.log(`${num} - ${val} = ${val - num}`)
    count++
}}else if(op == "/"){
    while(count<=10){
        console.log(`${num} / ${val} = ${val / num}`)
    count++
}}